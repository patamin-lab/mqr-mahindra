/** Mahindra brand red, used consistently across every PDF document's
 *  title/section-heading color (MQR's exportPdf.tsx, PM's
 *  maintenancePdf.tsx) - a single source so the two documents can never
 *  visually drift apart. */
export const PDF_BRAND_RED = '#9c1c1c';
